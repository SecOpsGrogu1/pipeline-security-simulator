# Vulnerable Dockerfile (comments prevent actual vulnerable packages from being installed)
FROM ubuntu:18.04

# Intentionally commented-out vulnerable packages:
# RUN apt-get update && \
#     apt-get install -y openssl=1.1.0g-2ubuntu4 && \
#     apt-get clean && rm -rf /var/lib/apt/lists/*

CMD ["bash"]