# This file intentionally contains vulnerabilities for scanner testing
SECRET_KEY = "mysecret"  # Uncomment this line to demo a pipeline failure (medium severity)
#result = eval("2+2")
#hash = md5(b"data").hexdigest()
