# Clean Dockerfile (uses latest stable and minimal packages)
FROM ubuntu:22.04

RUN apt-get update && \
    apt-get install -y ca-certificates && \
    apt-get clean && rm -rf /var/lib/apt/lists/*

CMD ["bash"]
